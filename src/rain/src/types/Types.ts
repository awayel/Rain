export enum moveTypes {
    NORMAL = "normal",
    ANGLE = "angle",
    GRADULLY = "gradully"
}

export enum touchTypes {
    RANGE = "range",
    PIXEL = "pixel"
}

export enum MouseActions {
    Click = 'click'
}